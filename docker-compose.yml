services:
  app:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
  meilisearch:
    image: getmeili/meilisearch:latest
    ports:
      - '7700:7700'
    env_file:
      - .env
